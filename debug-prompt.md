# 状态栏注入调试提示词

将以下内容设置为**系统提示词**，然后正常聊天，AI会在回复前先输出它看到的后台指令结构。

---

## 调试提示词（复制到系统提示词）

```
【调试模式】在正式回复角色内容之前，你必须先输出以下调试信息（用 ```code block``` 包裹）：

=== 后台指令结构 ===
1. 【系统提示词】（你收到的完整系统提示词内容）
2. 【重要强调】（如有强调提示，列出完整内容；无则写"无"）
3. 【状态栏指令】（如有状态栏相关指令，列出完整内容；无则写"无"）
4. 【中途格式提醒】（在对话历史中，是否收到过 system 角色的格式提醒？如有，列出完整内容和出现次数；无则写"无"）
5. 【Post-History指令】（在对话历史末尾，是否收到过 system/user 角色的格式强制要求？如有，列出完整内容；无则写"无"）
6. 【用户身份】（如有用户身份设定，列出完整内容；无则写"无"）
=== 调试信息结束 ===

输出调试信息后，再正常进行角色回复。如果启用了状态栏，回复末尾照常输出 <status>...</status>。
```

---

## 测试状态栏模板

建议先用这个简单模板测试：

```
当前地点、当前行动、当前穿搭、内心独白
```

## 测试步骤

1. 新建一个会话（避免旧上下文干扰）
2. 设置系统提示词为上面的调试提示词
3. 开启状态栏，模板用上面的简单模板
4. 开启强调提示，随便写点内容（如"保持可爱风格"）
5. 设置用户身份（如"主人"）
6. 发送任意消息，观察AI输出的调试信息

## 预期看到的调试输出

AI应该会输出类似：

```
=== 后台指令结构 ===
1. 【系统提示词】[你的角色卡内容]
2. 【重要强调】保持可爱风格
3. 【状态栏指令】【状态栏指令】每次回复末尾，请用 <status>...</status> 标签输出角色当前状态信息。状态栏应包含以下内容：当前地点、当前行动、当前穿搭、内心独白。请根据上下文合理填写数值和描述，保持角色一致性。示例格式：
<status>【角色状态】
当前地点：xxx
当前行动：xxx
当前穿搭：xxx
内心独白：xxx
</status>
4. 【中途格式提醒】[如有] 【格式提醒】回复末尾须包含 <status>...</status> 标签，内容包括：当前地点、当前行动、当前穿搭、内心独白 (出现1次)
5. 【Post-History指令】【格式提醒】你的每次回复必须在最末尾包含 <status>...</status> 标签的状态栏，内容包括：当前地点、当前行动、当前穿搭、内心独白。这是强制格式要求，不可省略。
6. 【用户身份】主人
=== 调试信息结束 ===
```

这样你就能看到AI实际接收到的所有注入内容了。
